<style>
	.sign * {
		font-family: Ubuntu;
		--fontColor: #3d3d3d;
	}
	.sign {
    width: 100%;
    padding: 25px 0;
    background: rgb(19 22 39 / 95%);
	}
	.sign__title {
		margin: 0;
		padding: 15px 0;
		font-size: var(--h1);
		text-align: center;
		margin: 15px auto;
		font-weight: normal;
	}
	.sign form {
		margin: 25px auto 40px auto;
		padding: 10px;
		width: 500px;
		display: flex;
		flex-direction: column;
		row-gap: 10px;
		border: 1px solid var(--fontColor);
	}
	.sign form * {
		padding: 10px;
	}
	.sign input {
    border: none;
    outline: none;
    padding: 15px;
    border-radius: 3px;
    box-sizing: border-box;
	}
	.sign form button {
		position: relative;
    background: #fff;
    padding: 15px;
    min-height: 54px;
    text-transform: uppercase;
    text-align: center;
    cursor: pointer;
    border-radius: 3px;
    transition: all .3s ease 0s;
    outline: none;
    box-sizing: border-box;
    overflow: hidden;
	}
	.sign form button:after {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		width: 0%;
		height: 100%;
		background: rgba(0, 0, 0, .7);
		display: flex;
		align-items: center;
		justify-content: center;
		transition: all .1s ease 0s;
	}
	.sign form button:hover {
		color: #fff;
	}
	.sign form button.signin-btn:hover:after {
		content: 'Войти';
		color: #fff;
		width: 100%;
	}
	.sign form button.signup-btn:hover:after {
		content: 'ЗАРЕГИСТРИРОВАТЬ';
		color: #fff;
		width: 100%;
	}

	.sign .role {
    position: relative;
    left: 40%;
    padding: 10px;
    margin: 45px 0;
    font-weight: normal;
    transition: all .1s ease 0s;
    transform: scale(0) translateX(100vw);
	}
	.sign .role._active {
		transform: scale(1) translateX(0);
	}
	.sign .sign__link {
    display: block;
    margin: 10px auto;
    color: #fff;
    text-align: center;
    text-decoration: underline;
	}
	.sign .sign .tooltip:hover {
		text-decoration: none;	
		color: #000;
	}
	.sign .sign__text {
		margin: 15px 0;
		text-align: center;	
	}
	.sign .tooltip {
		position: relative;	
		top: 20px;	
		opacity: 0;	
		transition: all .3s ease 0s;
		text-align: center;	
		z-index: 1;
	}
	.sign .tooltip._active {
		top: 0;	
		opacity: 1;	
	}
	.sign .info {
		position: relative;
    left: 0;
    width: 10px;
    padding: 15px;
    border-radius: 0 16px 16px 0;
    box-sizing: border-box;
	}
	.sign .info svg {
    position: relative;
    left: -7px;
	}

</style>
<script>	
		window.addEventListener('mouseover', (e) => {
			const tooltip = document.querySelector('.tooltip')
			if (e.target.closest('#tooltip') && tooltip) {
				tooltip.classList.add('_active')
			}
		})
</script>	